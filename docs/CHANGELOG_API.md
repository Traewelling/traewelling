# Changelog API

| 2022-12-21 | minor | `GET v1/statistics` | values `count` and `duration` are now converted to integer |
|------------|-------|---------------------|------------------------------------------------------------|
